import React, { useState } from "react";

const AddList = () =>{
    function addItem() {
        const newTask = { id: Date(), text }; 
        Setext([...text,newTask ]);
      }

    return(
        <>
            <button onClick={add}>Add More List </button>
        </>
    )

}
export default AddList;